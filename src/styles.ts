import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
    title: {
        fontSize: 24,
        marginBottom: 12,
        marginTop: 16,
        textAlign: 'center'
    }
})

export default globalStyles